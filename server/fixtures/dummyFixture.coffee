# The "||" notation doesn't work yet
Fixtures = if typeof Fixtures != 'undefined' then Fixtures else {}
Fixtures.dummyFixture = [
  {
    'foo': 'bar'
    'another': 'value'
  }
  {
    'foo': 'bar2'
    'another': 'value2'
  }
]
